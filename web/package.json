{
  "name": "finapp-web",
  "version": "1.0.0",
  "private": true,
  "description": "FinApp for the browser: a voice-driven expense tracker that installs to the iPhone home screen.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  }
}
