{
  "name": "finapp-sync-server",
  "version": "1.0.0",
  "description": "Companion sync server for FinApp. Holds the Plaid secret and bank access tokens so the iOS app never has to.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "plaid": "^27.0.0"
  }
}
